name: embedding
version: "1.0.0"

# Resource requirements
resources:
  cpu: 2 # Adjusted based on observed max usage of ~1 core with headroom
  memory: 4294967296 # 4Gi in bytes
  storage: 10737418240 # 10Gi for embedding model storage in bytes
